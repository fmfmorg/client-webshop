---
interface Props {
    className?:string;
}

const { className } = Astro.props as Props
---

<svg class={`w-full stroke-black fill-black ${className || ''}`.trim()}>
    <use href="#shop-logo" />
</svg>